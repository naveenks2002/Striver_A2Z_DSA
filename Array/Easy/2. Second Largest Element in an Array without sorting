1.  Brute force approach :

int print2largest(int arr[], int n) {
	    // code here
	    sort(arr,arr+n);
	    int max1 = arr[n-1];
	    int max2 = -1 ;
	    for(int i=n-2; i>=0; i--){
	        if(max2<max1){
	            max2 = arr[i];
	            return max2;
	        }
	    }
	    return -1;
	}
};


2.  Better approach :
